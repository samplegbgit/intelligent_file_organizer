# Intelligent File Organizer

A Python utility that scans a directory, analyzes files by type, size, and modification date, generates a summary report, and optionally organizes files into categorized folders.
